#include <cstdlib>

#include "Graph.h"

//------------------------------------------------------------------------------

Edge::Edge(vertex u, vertex v) : v1(u < v ? u : v), v2(u < v ? v : u),
	p((double)(rand()) / (double)(RAND_MAX)) { }

//------------------------------------------------------------------------------
